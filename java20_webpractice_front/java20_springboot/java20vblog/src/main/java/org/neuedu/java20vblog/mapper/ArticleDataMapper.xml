<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.neuedu.java20vblog.mapper.ArticleDataMapper">
    <select id="topTen" resultType="org.neuedu.java20vblog.model.Article">
        select id,title,views,publishTime
        from article
        where TIMESTAMPDIFF(month,publishTime,NOW()) = 0
        order by views DESC
        limit 0,10
    </select>
    <select id="articleCounts" resultType="org.neuedu.java20vblog.model.Category">
        select cname,count(a.id) as counts
        from article a
        join category c
        on a.cid = c.id
        GROUP BY cname
        order by counts desc
    </select>
</mapper>